{
  "name": "cognify",
  "version": "1.0.0",
  "description": "Student time tracking and productivity platform",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../extension && npm install"
  }
}
